;GIVEN EXPRESSION (X^2-Y^2) / X-Y


          OUTPUT 2500AD
.ORG 3000H
DATA SEGMENT
X DB 05H
Y DB 03H
RES DB 00H
DATA ENDS
CODE SEGMENT
	ASSUME CS:CODE,DS:DATA
	MOV AX,DATA
	MOV DS,AX
	MOV AL,X
	MUL AL
	MOV BX,AX
	MOV AL,Y
	MUL AL
	MOV CX,AX
	MOV AX,BX
	SUB AX,CX
	MOV BL,X
	SUB BL,Y
	DIV BL
	MOV RES,AL
CODE ENDS
END
