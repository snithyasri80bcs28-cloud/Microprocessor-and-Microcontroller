OUTPUT 2500AD
.ORG 2000H
DATA SEGMENT
LIST DB 01H, 02H, 04H, 08H
DATA ENDS
CODE SEGMENT
    ASSUME CS:CODE, DS:DATA
START:
    MOV AX, DATA
    MOV DS, AX
    MOV AL, 80H
    MOV DX, 0FFE6H
    OUT DX, AL
L3:
    MOV CH, 0DH
L2:
    MOV SI, OFFSET LIST
    MOV CL, 04H
L1:
    MOV AL, [SI]
    MOV DX, 0FFE0H
    OUT DX, AL
    CALL DELAY
    INC SI
    DEC CL
    JNZ L1
    DEC CH
    JNZ L2
    MOV CH, 0DH
L6:
    MOV SI, OFFSET LIST
    ADD SI, 03H
    MOV CL, 04H
L4:
    MOV AL, [SI]
    MOV DX, 0FFE0H
    OUT DX, AL
    CALL DELAY
    DEC SI
    DEC CL
    JNZ L4
    DEC CH
    JNZ L6
    MOV AL,08H
    MOV DX,FFE0H
    OUT DX,AL
    MOV AL,04H
    MOV DX,FFE0H
    OUT DX,AL
    JMP L3
DELAY:
    MOV BX, 0FFFFH
L5:
    DEC BX
    JNZ L5
    RET
CODE ENDS
END

